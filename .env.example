VITE_API_BASE=https://ec-course-api.hexschool.io/v2
VITE_API_PATH=rogan

# Agent events API：串到 ENSO 前台 (Next.js) 的 /api/events
# 預設 localhost:3000；跑 demo 時兩個 dev server 都要開：
#   ENSO-Frontend-demo: npm run dev   (→ http://localhost:3000)
#   ENSO-BackEnd:       npm run dev   (→ http://localhost:5173)
VITE_AGENT_API_BASE=http://localhost:3000
